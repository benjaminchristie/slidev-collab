/*
 * The entry Slidev loads for a deck's own styles.
 *
 * The rules themselves live next door in `index.css` as plain CSS, so that a
 * deck that wants a piece of this look can copy that one file and nothing
 * else. This module exists to point at it, which is the filename Slidev's
 * directory layout names for a project's styles.
 */
import './index.css'
